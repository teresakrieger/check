% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_aligned_tp.R
\name{load_aligned_tp}
\alias{load_aligned_tp}
\title{Load aligned Visium tissue positions}
\usage{
load_aligned_tp(
  file_dir,
  file_name = "aligned_tissue_positions",
  visium_sample = NULL,
  attr_name = "orientation_adjustments",
  file_type = "rds",
  L_capture_area = NULL,
  check_values = TRUE
)
}
\arguments{
\item{file_dir}{Character scalar. Directory from where the file(s) are loaded.}

\item{file_name}{Character scalar. Base filename without extension.
Defaults to \code{"aligned_tissue_positions"}.}

\item{visium_sample}{Optional character scalar. Sample identifier appended
to the base filename. Defaults to \code{NULL}.}

\item{attr_name}{Character scalar. Name of the attribute containing
orientation-adjustment information. Defaults to
\code{"orientation_adjustments"}.}

\item{file_type}{Character scalar specifying the file format:
Either \code{"rds"} (default) or \code{"csv"}.}

\item{L_capture_area}{Positive numeric scalar defining the side lengths
of the squared capture-area. Supported values are \code{6500} and
\code{11000}. Defaults to \code{NULL}.}

\item{check_values}{Logical scalar. Whether required values and coordinate
types should be validated and \code{NA} values should be excluded.
Defaults to \code{TRUE}.}
}
\value{
Data frame containing aligned Visium tissue positions and orientation
adjustments as attribute information.
}
\description{
Load and optionally validate the aligned Visium tissue positions and
orientation-adjustment metadata.
}
\examples{
\dontrun{
tp <- load_aligned_tp(
  file_dir = "/path/to/file",
  visium_sample = "Sample_1",
)
}

}
